<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>Casa-de-Popol - {% block title %}Welcome{% endblock %}</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css?family=Quicksand:wght@300;400;500&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Nanum+Gothic:wght@800;400;700&display=swap" rel="stylesheet">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        <!-- icons are from https://github.com/danklammer/bytesize-icons -->
    </head>
    <body>
        {% block body %}
            <div class="container-raw">
                <header class="row no-gutters">
                    <div class="col-xl-4 col-md-1 col-sm-12 display-at-sm"></div>
                    <div class="row col-xl-4 col-md-10 col-sm-12 text-center pipe build-a-wall bg-secondary no-gutters align-items-center">
                        <div class="col">
                            {% if app.user %}
                                <a href="{{ path('home') }}">
                                    <svg id="i-home" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="24" height="24" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                                        <path d="M12 20 L12 30 4 30 4 12 16 2 28 12 28 30 20 30 20 20 Z" />
                                    </svg>
                                </a>
                            {% endif %}
                        </div>
                        <div class="col-6">
                            <h1>{% block page_title %}{% endblock %}</h1>
                        </div>
                        <div class="col">
                            {% if app.user %}
                                <a href="{{ path('app_logout') }}">
                                    <svg id="i-signout" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="24" height="24" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                                        <path d="M28 16 L8 16 M20 8 L28 16 20 24 M11 28 L3 28 3 4 11 4" />
                                    </svg>
                                </a>
                            {% endif %}
                        </div>
                    </div>
                    <div class="col-xl-4 col-md-1 col-sm-12 display-at-sm"></div>
                </header>
                <main class="row no-gutters">
                    <div class="col-xl-4 col-md-1 col-sm-12 display-at-sm"></div>
                    <div class="col-xl-4 col-md-10 col-sm-12 pipe">
                        {% block page_content %}{% endblock %}
                    </div>
                    <div class="col-xl-4 col-md-1 col-sm-12 display-at-sm"></div>
                </main>
                <footer class="row">
                    {% block page_footer%}{% endblock %}
                </footer>
            </div>
        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
